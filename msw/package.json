{
  "name": "msw-turnos-poc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PoC universitaria: comparar mock manual de fetch (vi.stubGlobal) vs handlers de MSW 2 sobre el mismo cliente HTTP.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "compare": "tsx scripts/compare.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "msw": "^2.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
